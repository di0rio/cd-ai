# Fixture de eval: greet

Projeto mínimo com um teste que falha **de propósito**: `src/greet.ts` devolve só o nome, e o
teste espera `Olá, {nome}`.

Usado pela suíte da Fase 6 (`evals/tasks/greet.json`). Não entra no `bun run verify`.
