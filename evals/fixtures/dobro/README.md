# Fixture de eval: dobro

Projeto mínimo cujo teste importa `src/dobro.ts`, arquivo que **não existe**. O agente precisa
criar o módulo. O teste espera `dobro(3) === 6`.

Usado pela suíte da Fase 6 (`evals/tasks/dobro.json`). Não entra no `bun run verify`.
