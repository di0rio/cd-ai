{
  "name": "cd-ai",
  "private": true,
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "release:linux": "bun scripts/build-linux-release.ts",
    "check": "biome check .",
    "verify": "bun run check && bun run --cwd apps/desktop typecheck && bun run --cwd apps/desktop test && bun scripts/check-release-metadata.ts && cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.13",
    "@tauri-apps/cli": "^2.11.4"
  }
}
